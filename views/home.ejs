<% if (!user) { %>
	<p>Welcome! Please <a href="<%= prefix %>/login">log in</a>.</p>
<% } else { %>
	<p>Hello, <%= user.username %>. View your <a href="<%= prefix %>/profile">profile</a>.</p>
<% } %>
